<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- No native ActionBar: the Compose TopAppBar is the only app bar. Dark base
         with our window background so there is no light flash before Compose draws. -->
    <style name="Theme.DomainProbe" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
